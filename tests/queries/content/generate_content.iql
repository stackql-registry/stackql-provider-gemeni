SELECT
  JSON_EXTRACT(candidates, '$[0].content.parts[0].text') AS reply,
  JSON_EXTRACT(usageMetadata, '$.totalTokenCount') AS total_tokens,
  modelVersion
FROM gemini.models.content
WHERE modelsId = 'mock-model-1'
AND contents = '[{"parts":[{"text":"hello"}]}]';
