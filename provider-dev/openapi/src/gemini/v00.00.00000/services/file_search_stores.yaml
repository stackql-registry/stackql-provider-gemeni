openapi: 3.0.3
info:
  title: file_search_stores API
  description: gemini file_search_stores API
  version: v1beta
paths:
  /v1beta/fileSearchStores:
    get:
      operationId: fileSearchStores.list
      description: Lists all `FileSearchStores` owned by the user.
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListFileSearchStoresResponse"
      parameters:
        - in: query
          name: pageSize
          schema:
            type: integer
            format: int32
          description: Optional. The maximum number of `FileSearchStores` to return (per page). The service may return fewer `FileSearchStores`. If unspecified, at most 10 `FileSearchStores` will be returned. The maximum size limit is 20 `FileSearchStores` per page.
        - in: query
          name: pageToken
          schema:
            type: string
          description: Optional. A page token, received from a previous `ListFileSearchStores` call. Provide the `next_page_token` returned in the response as an argument to the next request to retrieve the next page. When paginating, all other parameters provided to `ListFileSearchStores` must match the call that provided the page token.
    post:
      operationId: fileSearchStores.create
      description: Creates an empty `FileSearchStore`.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FileSearchStore"
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileSearchStore"
  /v1beta/fileSearchStores/{fileSearchStoresId}:
    get:
      operationId: fileSearchStores.get
      description: Gets information about a specific `FileSearchStore`.
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileSearchStore"
      parameters:
        - in: path
          name: fileSearchStoresId
          required: true
          schema:
            type: string
    delete:
      operationId: fileSearchStores.delete
      description: Deletes a `FileSearchStore`.
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Empty"
      parameters:
        - in: path
          name: fileSearchStoresId
          required: true
          schema:
            type: string
        - in: query
          name: force
          schema:
            type: boolean
          description: Optional. If set to true, any `Document`s and objects related to this `FileSearchStore` will also be deleted. If false (the default), a `FAILED_PRECONDITION` error will be returned if `FileSearchStore` contains any `Document`s.
  /v1beta/fileSearchStores/{fileSearchStoresId}:importFile:
    post:
      operationId: fileSearchStores.importFile
      description: Imports a `File` from File Service to a `FileSearchStore`.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ImportFileRequest"
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Operation"
      parameters:
        - in: path
          name: fileSearchStoresId
          required: true
          schema:
            type: string
  /v1beta/fileSearchStores/{fileSearchStoresId}/operations/{operationsId}:
    get:
      operationId: fileSearchStores.operations.get
      description: Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Operation"
      parameters:
        - in: path
          name: fileSearchStoresId
          required: true
          schema:
            type: string
        - in: path
          name: operationsId
          required: true
          schema:
            type: string
  /v1beta/fileSearchStores/{fileSearchStoresId}/documents/{documentsId}:
    delete:
      operationId: fileSearchStores.documents.delete
      description: Deletes a `Document`.
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Empty"
      parameters:
        - in: path
          name: fileSearchStoresId
          required: true
          schema:
            type: string
        - in: path
          name: documentsId
          required: true
          schema:
            type: string
        - in: query
          name: force
          schema:
            type: boolean
          description: Optional. If set to true, any `Chunk`s and objects related to this `Document` will also be deleted. If false (the default), a `FAILED_PRECONDITION` error will be returned if `Document` contains any `Chunk`s.
    get:
      operationId: fileSearchStores.documents.get
      description: Gets information about a specific `Document`.
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Document"
      parameters:
        - in: path
          name: fileSearchStoresId
          required: true
          schema:
            type: string
        - in: path
          name: documentsId
          required: true
          schema:
            type: string
  /v1beta/fileSearchStores/{fileSearchStoresId}/documents:
    get:
      operationId: fileSearchStores.documents.list
      description: Lists all `Document`s in a `Corpus`.
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListDocumentsResponse"
      parameters:
        - in: path
          name: fileSearchStoresId
          required: true
          schema:
            type: string
        - in: query
          name: pageSize
          schema:
            type: integer
            format: int32
          description: Optional. The maximum number of `Document`s to return (per page). The service may return fewer `Document`s. If unspecified, at most 10 `Document`s will be returned. The maximum size limit is 20 `Document`s per page.
        - in: query
          name: pageToken
          schema:
            type: string
          description: Optional. A page token, received from a previous `ListDocuments` call. Provide the `next_page_token` returned in the response as an argument to the next request to retrieve the next page. When paginating, all other parameters provided to `ListDocuments` must match the call that provided the page token.
  /v1beta/fileSearchStores/{fileSearchStoresId}/upload/operations/{operationsId}:
    get:
      operationId: fileSearchStores.upload.operations.get
      description: Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Operation"
      parameters:
        - in: path
          name: fileSearchStoresId
          required: true
          schema:
            type: string
        - in: path
          name: operationsId
          required: true
          schema:
            type: string
components:
  schemas:
    ListFileSearchStoresResponse:
      id: ListFileSearchStoresResponse
      description: Response from `ListFileSearchStores` containing a paginated list of `FileSearchStores`. The results are sorted by ascending `file_search_store.create_time`.
      type: object
      properties:
        fileSearchStores:
          items:
            $ref: "#/components/schemas/FileSearchStore"
          type: array
          description: The returned rag_stores.
        nextPageToken:
          type: string
          description: A token, which can be sent as `page_token` to retrieve the next page. If this field is omitted, there are no more pages.
    FileSearchStore:
      id: FileSearchStore
      description: A `FileSearchStore` is a collection of `Document`s.
      type: object
      properties:
        createTime:
          readOnly: true
          format: google-datetime
          type: string
          description: Output only. The Timestamp of when the `FileSearchStore` was created.
        failedDocumentsCount:
          type: string
          readOnly: true
          format: int64
          description: Output only. The number of documents in the `FileSearchStore` that have failed processing.
        pendingDocumentsCount:
          description: Output only. The number of documents in the `FileSearchStore` that are being processed.
          readOnly: true
          format: int64
          type: string
        name:
          type: string
          readOnly: true
          description: 'Output only. Immutable. Identifier. The `FileSearchStore` resource name. It is an ID (name excluding the "fileSearchStores/" prefix) that can contain up to 40 characters that are lowercase alphanumeric or dashes (-). It is output only. The unique name will be derived from `display_name` along with a 12 character random suffix. Example: `fileSearchStores/my-awesome-file-search-store-123a456b789c` If `display_name` is not provided, the name will be randomly generated.'
        embeddingModel:
          type: string
          description: "Optional. The embedding model to use for the `FileSearchStore`. The model's resource name. This serves as an ID for the Model to use. Format: `models/{model}`. If not specified, the default embedding model will be used."
        displayName:
          type: string
          description: 'Optional. The human-readable display name for the `FileSearchStore`. The display name must be no more than 512 characters in length, including spaces. Example: "Docs on Semantic Retriever"'
        updateTime:
          type: string
          readOnly: true
          format: google-datetime
          description: Output only. The Timestamp of when the `FileSearchStore` was last updated.
        activeDocumentsCount:
          description: Output only. The number of documents in the `FileSearchStore` that are active and ready for retrieval.
          readOnly: true
          format: int64
          type: string
        sizeBytes:
          description: Output only. The size of raw bytes ingested into the `FileSearchStore`. This is the total size of all the documents in the `FileSearchStore`.
          type: string
          readOnly: true
          format: int64
    Empty:
      id: Empty
      description: "A generic empty message that you can re-use to avoid defining duplicated empty messages in your APIs. A typical example is to use it as the request or the response type of an API method. For instance: service Foo { rpc Bar(google.protobuf.Empty) returns (google.protobuf.Empty); }"
      type: object
      properties: {}
    ImportFileRequest:
      type: object
      properties:
        fileName:
          description: "Required. The name of the `File` to import. Example: `files/abc-123`"
          type: string
        chunkingConfig:
          description: Optional. Config for telling the service how to chunk the file. If not provided, the service will use default parameters.
          $ref: "#/components/schemas/ChunkingConfig"
        customMetadata:
          description: Custom metadata to be associated with the file.
          items:
            $ref: "#/components/schemas/CustomMetadata"
          type: array
      id: ImportFileRequest
      description: Request for `ImportFile` to import a File API file with a `FileSearchStore`.
    Operation:
      id: Operation
      description: This resource represents a long-running operation that is the result of a network API call.
      type: object
      properties:
        response:
          description: The normal, successful response of the operation. If the original method returns no data on success, such as `Delete`, the response is `google.protobuf.Empty`. If the original method is standard `Get`/`Create`/`Update`, the response should be the resource. For other methods, the response should have the type `XxxResponse`, where `Xxx` is the original method name. For example, if the original method name is `TakeSnapshot()`, the inferred response type is `TakeSnapshotResponse`.
          type: object
        done:
          type: boolean
          description: If the value is `false`, it means the operation is still in progress. If `true`, the operation is completed, and either `error` or `response` is available.
        name:
          type: string
          description: The server-assigned name, which is only unique within the same service that originally returns it. If you use the default HTTP mapping, the `name` should be a resource name ending with `operations/{unique_id}`.
        error:
          description: The error result of the operation in case of failure or cancellation.
          $ref: "#/components/schemas/Status"
        metadata:
          type: object
          description: Service-specific metadata associated with the operation. It typically contains progress information and common metadata such as create time. Some services might not provide such metadata. Any method that returns a long-running operation should document the metadata type, if any.
    Document:
      type: object
      properties:
        createTime:
          readOnly: true
          format: google-datetime
          type: string
          description: Output only. The Timestamp of when the `Document` was created.
        displayName:
          type: string
          description: 'Optional. The human-readable display name for the `Document`. The display name must be no more than 512 characters in length, including spaces. Example: "Semantic Retriever Documentation"'
        customMetadata:
          description: Optional. User provided custom metadata stored as key-value pairs used for querying. A `Document` can have a maximum of 20 `CustomMetadata`.
          type: array
          items:
            $ref: "#/components/schemas/CustomMetadata"
        updateTime:
          description: Output only. The Timestamp of when the `Document` was last updated.
          type: string
          readOnly: true
          format: google-datetime
        mimeType:
          description: Output only. The mime type of the Document.
          type: string
          readOnly: true
        sizeBytes:
          readOnly: true
          format: int64
          type: string
          description: Output only. The size of raw bytes ingested into the Document.
        name:
          type: string
          description: 'Immutable. Identifier. The `Document` resource name. The ID (name excluding the "fileSearchStores/*/documents/" prefix) can contain up to 40 characters that are lowercase alphanumeric or dashes (-). The ID cannot start or end with a dash. If the name is empty on create, a unique name will be derived from `display_name` along with a 12 character random suffix. Example: `fileSearchStores/{file_search_store_id}/documents/my-awesome-doc-123a456b789c`'
        state:
          description: Output only. Current state of the `Document`.
          enumDescriptions:
            - The default value. This value is used if the state is omitted.
            - Some `Chunks` of the `Document` are being processed (embedding and vector storage).
            - All `Chunks` of the `Document` is processed and available for querying.
            - Some `Chunks` of the `Document` failed processing.
          type: string
          enum:
            - STATE_UNSPECIFIED
            - STATE_PENDING
            - STATE_ACTIVE
            - STATE_FAILED
          readOnly: true
      id: Document
      description: A `Document` is a collection of `Chunk`s.
    ListDocumentsResponse:
      id: ListDocumentsResponse
      description: Response from `ListDocuments` containing a paginated list of `Document`s. The `Document`s are sorted by ascending `document.create_time`.
      type: object
      properties:
        documents:
          description: The returned `Document`s.
          items:
            $ref: "#/components/schemas/Document"
          type: array
        nextPageToken:
          description: A token, which can be sent as `page_token` to retrieve the next page. If this field is omitted, there are no more pages.
          type: string
    ChunkingConfig:
      type: object
      properties:
        whiteSpaceConfig:
          description: White space chunking configuration.
          $ref: "#/components/schemas/WhiteSpaceConfig"
      id: ChunkingConfig
      description: Parameters for telling the service how to chunk the file. inspired by google3/cloud/ai/platform/extension/lib/retrieval/config/chunker_config.proto
    CustomMetadata:
      id: CustomMetadata
      description: User provided metadata stored as key-value pairs.
      type: object
      properties:
        stringValue:
          description: The string value of the metadata to store.
          type: string
        numericValue:
          type: number
          format: float
          description: The numeric value of the metadata to store.
        key:
          type: string
          description: Required. The key of the metadata to store.
        stringListValue:
          description: The StringList value of the metadata to store.
          $ref: "#/components/schemas/StringList"
    Status:
      id: Status
      description: "The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors)."
      type: object
      properties:
        message:
          type: string
          description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the google.rpc.Status.details field, or localized by the client.
        code:
          type: integer
          format: int32
          description: The status code, which should be an enum value of google.rpc.Code.
        details:
          description: A list of messages that carry the error details. There is a common set of message types for APIs to use.
          type: array
          items:
            type: object
    WhiteSpaceConfig:
      type: object
      properties:
        maxTokensPerChunk:
          description: "Maximum number of tokens per chunk. Tokens are defined as words for this chunking algorithm. Note: we are defining tokens as words split by whitespace as opposed to the output of a tokenizer. The context window of the latest gemini embedding model as of 2025-04-17 is currently 8192 tokens. We assume that the average word is 5 characters. Therefore, we set the upper limit to 2**9, which is 512 words, or 2560 tokens, assuming worst case a character per token. This is a conservative estimate meant to prevent context window overflow."
          type: integer
          format: int32
        maxOverlapTokens:
          format: int32
          type: integer
          description: Maximum number of overlapping tokens between two adjacent chunks.
      id: WhiteSpaceConfig
      description: Configuration for a white space chunking algorithm [white space delimited].
    StringList:
      type: object
      properties:
        values:
          items:
            type: string
          type: array
          description: The string values of the metadata to store.
      id: StringList
      description: User provided string values assigned to a single metadata key.
    StackqlExecResult:
      type: object
      description: Synthetic dispatch-result envelope for EXEC-only methods (see post-pass step 5).
      properties:
        result:
          type: string
          description: Raw response payload.
        items:
          type: array
          items:
            type: string
  x-stackQL-resources:
    file_search_stores:
      id: gemini.file_search_stores.file_search_stores
      name: file_search_stores
      title: File Search Stores
      methods:
        list:
          operation:
            $ref: "#/paths/~1v1beta~1fileSearchStores/get"
          response:
            mediaType: application/json
            openAPIDocKey: "200"
            objectKey: $.fileSearchStores
          config:
            pagination:
              requestToken:
                key: pageToken
                location: query
              responseToken:
                key: $.nextPageToken
                location: body
            queryParamPushdown:
              top:
                paramName: pageSize
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: "#/paths/~1v1beta~1fileSearchStores/post"
          response:
            mediaType: application/json
            openAPIDocKey: "200"
          request:
            mediaType: application/json
        get:
          operation:
            $ref: "#/paths/~1v1beta~1fileSearchStores~1{fileSearchStoresId}/get"
          response:
            mediaType: application/json
            openAPIDocKey: "200"
        delete:
          operation:
            $ref: "#/paths/~1v1beta~1fileSearchStores~1{fileSearchStoresId}/delete"
          response:
            mediaType: application/json
            openAPIDocKey: "200"
        import_file:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: "#/paths/~1v1beta~1fileSearchStores~1{fileSearchStoresId}:importFile/post"
          response:
            mediaType: application/json
            openAPIDocKey: "200"
            schema_override:
              $ref: "#/components/schemas/StackqlExecResult"
          request:
            mediaType: application/json
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/file_search_stores/methods/get"
          - $ref: "#/components/x-stackQL-resources/file_search_stores/methods/list"
        insert:
          - $ref: "#/components/x-stackQL-resources/file_search_stores/methods/create"
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/file_search_stores/methods/delete"
        replace: []
    operations:
      id: gemini.file_search_stores.operations
      name: operations
      title: Operations
      methods:
        get:
          operation:
            $ref: "#/paths/~1v1beta~1fileSearchStores~1{fileSearchStoresId}~1operations~1{operationsId}/get"
          response:
            mediaType: application/json
            openAPIDocKey: "200"
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/operations/methods/get"
        insert: []
        update: []
        delete: []
        replace: []
    documents:
      id: gemini.file_search_stores.documents
      name: documents
      title: Documents
      methods:
        delete:
          operation:
            $ref: "#/paths/~1v1beta~1fileSearchStores~1{fileSearchStoresId}~1documents~1{documentsId}/delete"
          response:
            mediaType: application/json
            openAPIDocKey: "200"
        get:
          operation:
            $ref: "#/paths/~1v1beta~1fileSearchStores~1{fileSearchStoresId}~1documents~1{documentsId}/get"
          response:
            mediaType: application/json
            openAPIDocKey: "200"
        list:
          operation:
            $ref: "#/paths/~1v1beta~1fileSearchStores~1{fileSearchStoresId}~1documents/get"
          response:
            mediaType: application/json
            openAPIDocKey: "200"
            objectKey: $.documents
          config:
            pagination:
              requestToken:
                key: pageToken
                location: query
              responseToken:
                key: $.nextPageToken
                location: body
            queryParamPushdown:
              top:
                paramName: pageSize
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/documents/methods/get"
          - $ref: "#/components/x-stackQL-resources/documents/methods/list"
        insert: []
        update: []
        delete:
          - $ref: "#/components/x-stackQL-resources/documents/methods/delete"
        replace: []
    upload_operations:
      id: gemini.file_search_stores.upload_operations
      name: upload_operations
      title: Upload Operations
      methods:
        get:
          operation:
            $ref: "#/paths/~1v1beta~1fileSearchStores~1{fileSearchStoresId}~1upload~1operations~1{operationsId}/get"
          response:
            mediaType: application/json
            openAPIDocKey: "200"
      sqlVerbs:
        select:
          - $ref: "#/components/x-stackQL-resources/upload_operations/methods/get"
        insert: []
        update: []
        delete: []
        replace: []
servers:
  - url: https://generativelanguage.googleapis.com
